<a href="#main" class="skip-link">{{ meta.skipContent }}</a>

<header class="site-head text-invert">
  <div class="wrapper | ontop spot-color-primary">
    {% set activePage = page.url | url %}
   <a href="/" class="logo site-head__brand | no-underline" {% if activePage === '/' %} aria-current="page" {% endif %}>
      <span>{{ meta.siteName }}<span class="logo-small-full"><br />Multi-Agent Transport Simulation</span></span>
    </a>

    {% include "partials/menu.njk" %}
  </div>
</header>
